# 跨專案修改請求

> 綜合大腦發現需要修改但不直接動手的項目，由對應 session 執行

---

## 待處理

### 請求 #1：宏觀日報 — 恢復 generate_full_report.py 新版
- **專案**：Claude-每日宏觀日報
- **問題**：綜合大腦在清除 cbe566 時，從路徑1 force push 覆蓋了路徑2 的新版程式碼
- **被影響的檔案**：`scripts/generate_full_report.py`（新聞回到英文標題、沒有分類標籤、沒有敘事摘要）
- **恢復方式**：`git checkout 07d0ee7 -- scripts/generate_full_report.py` 然後 commit + push
- **狀態**：⏳ 等待宏觀日報 session 處理

### 請求 #2：股票量化 — PDF 中文亂碼 + 空白頁 + email 修正
- **專案**：Claude-股票量化數據研究網
- **優先級**：🔴 致命（PDF 不可用）
- **問題**：`generate_report_pdf.py` 產出的 PDF 所有中文都是 ■■■ 方塊
- **根因**：GitHub Actions（Linux）上沒有中文字體，ReportLab 找不到 TTF
- **需要修改的檔案**：`generate_report_pdf.py`
- **具體修復**：
  1. **中文字體**：在 workflow 安裝 `fonts-noto-cjk`（已有），但程式碼要註冊字體：
     ```python
     from reportlab.pdfbase import pdfmetrics
     from reportlab.pdfbase.ttfonts import TTFont
     import platform

     def register_chinese_font():
         if platform.system() == "Linux":
             candidates = [
                 ("/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc", "NotoSansCJK"),
                 ("/usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc", "NotoSansCJK"),
             ]
         else:
             candidates = [
                 ("/Library/Fonts/Arial Unicode.ttf", "ArialUnicode"),
                 ("/System/Library/Fonts/PingFang.ttc", "PingFang"),
             ]
         for path, name in candidates:
             if os.path.exists(path):
                 pdfmetrics.registerFont(TTFont(name, path))
                 return name
         from reportlab.pdfbase.cidfonts import UnicodeCIDFont
         pdfmetrics.registerFont(UnicodeCIDFont('STSong-Light'))
         return 'STSong-Light'
     ```
  2. **Emoji 清理**：所有文字經過 `remove_emoji()` 清理 BMP 以外字元
  3. **空白頁**：第 6 頁只有署名，超過半頁的元素不用 KeepTogether
  4. **Email**：署名區的 cbe566@gmail.com 改成 backup901012@gmail.com
- **參考**：`claude-綜合大腦解決大方向問題/文書製作標準.md` 有完整的程式碼範例
- **狀態**：⏳ 等待股票量化 session 處理

---

## 已處理

（無）
